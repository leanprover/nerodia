[project]
name = "test"
version = "0.0.0"
requires-python = ">=3.14"

[build-system]
# opt into `setuptools-lean` prereleases for testing
requires = ["setuptools", "setuptools-lean>=0.dev0"]
build-backend = "setuptools.build_meta"

[[tool.setuptools-lean.ext-modules]]
lean-module = "Test"
